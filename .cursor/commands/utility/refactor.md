---
description: "Refactor/improve existing code (maintain behavior; arch+perf+clarity; small diffs)"
---
IN:FEATURE=<opt name>;TARGETS=<file list|module list|glob>;CTX=<opt docs/features/<slug>.md|paste>;GOAL=<brief>;FOCUS=<opt:arch|perf|queries|async|errors|boundaries|naming|readability>;MODE=plan+patch|patch(def plan+patch)
RULES:-Refactor only: preserve behavior/outputs/contracts unless GOAL explicitly allows a change; no feature work.-Follow project standards; reuse existing patterns; no new deps unless explicitly asked.-No inline comments in code(no #, no /* */).Docs only via @doc/@moduledoc when truly needed.-Small PR discipline: minimal diffs; avoid rewrites; if large, propose Phase1/2/3 in PLAN; implement Phase1 only unless user says otherwise.-Arch order for changes: entry->orchestration->domain->persistence->context/adapters; don't leak IO into domain.-Focus guidance: arch=boundaries+module roles; perf=allocs+hot paths; queries=composable Ecto+no N+1; async=bounded Task+Oban hygiene; errors=consistent {:ok,_}/{:error,_}; boundaries=ports/adapters; naming=intent; readability=smaller funcs/less nesting.-Control flow: prefer function heads+pattern matches; with for linear happy path; case for tagged tuples; cond rare.-Context structure: each context follows facade(<context>.ex with defdelegate) + <context>/query.ex (reads) + <context>/write.ex (writes) + <context>/schemas/ (Ecto schemas). Repo reads in Query, Repo writes in Write, no Repo in schemas.-Queries: avoid Repo in loops; batch/preload; select only needed fields; factor query builders (queryable in/out) inside <Context>.Query; explicit tx for multi-write.-Async/Oban: remove unbounded concurrency; ensure idempotency; respect existing queues/limits; avoid raising queue sizes.-Do not change tests here (separate command).If refactor risks breaking tests, flag in PLAN with suggested test updates (don't implement).
OUTPUT:MODE=plan+patch => PLAN(3-12 bullets,max 1200 chars) then unified diff; MODE=patch => diff only.No commentary outside contract.
PLAN fmt(tight):-Goal:-Focus:-Targets:-Key refactors(extract/rename/split):-Risk(contract/perf):-Follow-up cmds(tests/docs/logs)
POST(ask mode):Prompt user to run quality gates: mix format && mix compile --warnings-as-errors && mix credo --strict

EX: /refactor FEATURE="Pipeline" TARGETS="lib/elder/pipeline/**.ex" CTX="docs/features/pipeline.md" GOAL="reduce nesting + extract query builders" FOCUS=queries MODE=plan+patch
